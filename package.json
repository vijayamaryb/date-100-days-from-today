{
  "name": "date-after-100-days-from-today",
  "version": "1.0.0",
  "description": "Given an `app.js` file, write an API with path `/` using express JS to send the date after 100 days from today as a response in `DD/MM/YYYY` format.",
  "main": "app.js",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "date-fns": "2.19.0",
    "express": "4.17.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": []
}
